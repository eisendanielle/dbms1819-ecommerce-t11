<div class="container jumbotron email">
    <h3 align="center">{{message}}</h3>

    <div class="form-group">
        <div class="submit">
            <form method="GET" action="/products/{{PID}}">
                <button type="submit" class="btn submitButton" title="Go Back">
                    Go Back to Products
                </button>
            </form>
        </div>
    </div>
</div>

<style>
    .submit {
        padding-top: 10px;
        position: relative;
        max-width: 100%;
        text-align: end;
    }

    .submitButton:hover {
        transition: all 0.4s ease-in-out;
        box-shadow: 0 5px 10px rgba(76, 102, 119);
    }

    .submitButton {
        background: transparent;
        border: 2px solid rgb(61, 79, 93);
        color: rgb(61, 79, 93);
        letter-spacing: 0.04em;
        text-transform: uppercase;
        font-size: 85%;
        list-style: none;
    }
</style>
<div class="container brands">
    <div class="row">
        {{#each data}}
        <div class="col-12">
            {{this.brand_name}}
            <div class="col" style="padding-left: 5px;">{{this.brand_description}} </div>
        </div>
        {{/each}}
    </div>
</div>